export interface Note {
  title: string;
  content: string;
  // type: 'normal' | 'list' | 'drawing' | 'image';
  // selected: boolean;
  // pinned: boolean;
  // archived: boolean;
  // backgroundColor: string;
  // date: Date;
}
